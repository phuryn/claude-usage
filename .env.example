# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Required for live access: generate with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DASHBOARD_TOKEN=

# Optional overrides
# PORT=8080
# ABACUS_MARKUP=1.0
# BUDGET_DAILY=50
# BUDGET_MONTHLY=500
